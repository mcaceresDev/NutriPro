import { Request, Response } from "express";

class UserController {

    getUsers = () => {
        try {

            
        } catch (error:any) {
            console.log(error);            
        }
    }
    
    addUser = () => {
        try {

            
        } catch (error:any) {
            console.log(error);            
        }
    }
}

export default new UserController()